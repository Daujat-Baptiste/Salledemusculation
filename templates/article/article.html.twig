{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    {{ include('menuAdmin.html.twig') }}
    <div class="container">
            <div class="row">
                {{ article.nom }}
            </div>
        <div class="row">
            {{ article.auteur }}
        </div>
        <div class="row">
            {{ article.contenu }}
        </div>
    </div>
{% endblock %}