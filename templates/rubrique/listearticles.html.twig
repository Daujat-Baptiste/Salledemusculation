{% extends 'base.html.twig' %}

{% block title %}Rubriques{% endblock %}

{% block body %}
    <div class="container">
        {% for article in  articles %}
            <div class="row">
                <a href="{{ path('articleinfo',{id : article.id}) }}">{{ article.nom }}</a>
            </div>
        {% endfor %}
    </div>
{% endblock %}