{% extends 'base.html.twig' %}

{% block title %}Rubriques{% endblock %}

{% block body %}
    {{ include('menuAdmin.html.twig') }}
    <div class="container">
        {% for rubrique in  rubriques %}
            <div class="row">
                <a href="{{ path('listearticles',{id : rubrique.id}) }}">{{ rubrique.nom }}</a>
            </div>
        {% endfor %}
    </div>
{% endblock %}