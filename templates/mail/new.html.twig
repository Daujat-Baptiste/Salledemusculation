{% extends 'base.html.twig' %}

{% block title %}New Mail{% endblock %}

{% block body %}
    <h1>Ecrire un Email</h1>
    <div class="container">
        {{ include('mail/_form.html.twig') }}
        <br><br>
        <div class="text-center">
            <br><br>
            <a href="{{ path('mail_index') }}">Revenir à la liste</a>
        </div>
    </div>
{% endblock %}
