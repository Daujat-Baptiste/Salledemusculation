<footer class="footer mt-auto py-3 d-flex justify-content-around">
    <table>
        <thead>
        <tr>
            <th>
                <nav class="navbar navbar-expand-lg navbar-light">
                    <button class="navbar-toggler" type="button" data-toggle="collapse"
                            data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                            aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>

                    <div class="collapse navbar-collapse" id="navbarSupportedContent">
                        <ul class="navbar-nav mr-auto">
                            <li>
                                <a class="nav-link text-light" href="{{ path('planning') }}">Planning</a>
                            </li>
                            <li>
                                <a class="nav-link text-light" href="{{ path('contact') }}">Contact</a>
                            </li>
                        </ul>

                    </div>
                </nav>
            </th>
        </tr>
        </thead>

        <tbody>
        <tr>
            <th>
                <div class="row align-items-center">
                    <small class="row align-items-center">
                        @ {{ 'now' | date('Y') }} &middot;
                        BTS SIO 2 - Daujat_B & Charton_G
                    </small>
                </div>
            </th>
        </tr>
        </tbody>
    </table>
</footer>